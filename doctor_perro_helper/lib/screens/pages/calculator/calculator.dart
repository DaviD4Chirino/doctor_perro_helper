import 'package:flutter/material.dart';

class DolarCalculator extends StatelessWidget {
  const DolarCalculator({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Calculator"),
      ),
    );
  }
}
