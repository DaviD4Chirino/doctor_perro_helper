import 'package:flutter/material.dart';

class SwipeablePack extends StatelessWidget {
  const SwipeablePack({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
